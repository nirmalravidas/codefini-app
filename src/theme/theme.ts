// src/constants/theme.ts

import { appName, logo } from './constants/brand';
import { lightColors, darkColors } from './constants/colors';

export const theme = {
  appName,
  logo,
  lightColors,
  darkColors,
};

// Optional named exports (if you want to import them individually)
export {
  appName,
  logo,
  lightColors,
  darkColors,
};

